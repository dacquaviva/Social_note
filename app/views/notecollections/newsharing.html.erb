<%= form_with url: create_sharing_notecollection_path do |form| %>

<div class="field">
<%= collection_check_boxes(:users, :user_ids, @user, :id, :email) %>
</div>

<div class="actions">
    <%= form.submit %>
</div>
<% end %>
